// React core libraries
import React from 'react';
import ReactDOM from 'react-dom';

// Routes
import routes from './routes';

// Styles
import './css/main.css';

// Render the routes to the root element
ReactDOM.render(routes, document.querySelector('#root'));
